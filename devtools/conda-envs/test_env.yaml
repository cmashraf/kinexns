name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  # Dependencies
  - numpy 
  - pandas
  - assimulo
  - rdkit
  - PIL
  - matplotlib
