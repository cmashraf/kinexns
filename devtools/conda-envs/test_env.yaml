name: test
channels:
  - rdkit
  - conda-forge
  - openbabel
dependencies:
    # Base depends
  - python>=3.6
    # Testing
  - pytest
  - pytest-cov
  - codecov

  # Dependencies
  - numpy 
  - pandas
  - assimulo
  - rdkit
  - PIL
  - matplotlib
